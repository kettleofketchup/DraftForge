{
  "_doc": [
    "DraftForge Loki datasource — source of truth for derived fields.",
    "",
    "Apply with `docs/dev/telemetry/datasources/apply.sh` (uses gcx auth context).",
    "",
    "Portability: this file targets Grafana Cloud HTTP API. For a self-hosted",
    "Grafana (e.g. eventual home-cluster move), copy the `jsonData.derivedFields`",
    "block into `provisioning/datasources/loki.yaml` under `datasources[].jsonData.",
    "derivedFields`. The schema is identical; only the wrapper format differs.",
    "",
    "Secrets (basicAuthPassword) are NOT in this file. They stay configured",
    "server-side via the Grafana UI once; subsequent PUTs preserve them as",
    "long as `secureJsonData` is omitted from the body."
  ],
  "uid": "grafanacloud-logs",
  "name": "grafanacloud-kettle-logs",
  "type": "loki",
  "access": "proxy",
  "url": "https://logs-prod-042.grafana.net",
  "basicAuth": true,
  "basicAuthUser": "1458154",
  "isDefault": false,
  "jsonData": {
    "timeout": "300",
    "streamingEnabled": {
      "search": true
    },
    "derivedFields": [
      {
        "_doc": "Click any traceID in a log line -> open the trace in Tempo.",
        "name": "traceID",
        "matcherType": "regex",
        "matcherRegex": "[tT]race_?[iI][dD]\"?[:=]\"?(\\w+)",
        "datasourceUid": "grafanacloud-traces",
        "url": "${__value.raw}"
      },
      {
        "_doc": "Same as above, but pulled from the `traceID` OTel attribute label instead of regex on the body.",
        "name": "traceID (field)",
        "matcherType": "label",
        "matcherRegex": "[tT]race_?[iI][dD]",
        "datasourceUid": "grafanacloud-traces",
        "url": "${__value.raw}"
      },
      {
        "_doc": "Click any ws_conn_id -> internal Loki query showing the full lifetime of that WebSocket connection (connect, every heartbeat, every message, disconnect). Uses an internal link (`datasourceUid` = same Loki) so you stay in the Explore view with the conn_id pre-filtered.",
        "name": "wsConnId",
        "matcherType": "regex",
        "matcherRegex": "\"?ws[._]conn_id\"?\\s*[:=]\\s*\"?([a-f0-9-]+)",
        "datasourceUid": "grafanacloud-logs",
        "url": "{service_name=\"backend\"} | json | ws_conn_id=\"${__value.raw}\""
      },
      {
        "_doc": "Click any user_id -> internal Loki query showing every log emitted for that user across all systems. Useful for support requests and per-user debugging.",
        "name": "userId",
        "matcherType": "regex",
        "matcherRegex": "\"?user_id\"?\\s*[:=]\\s*\"?(\\d+)",
        "datasourceUid": "grafanacloud-logs",
        "url": "{service_name=~\"backend|celery-worker\"} | json | user_id=\"${__value.raw}\""
      },
      {
        "_doc": "Click any draft_id -> all logs related to that herodraft session across the websocket, bracket, and herodraft systems.",
        "name": "draftId",
        "matcherType": "regex",
        "matcherRegex": "\"?draft_id\"?\\s*[:=]\\s*\"?(\\d+)",
        "datasourceUid": "grafanacloud-logs",
        "url": "{service_name=~\"backend|celery-worker\"} | json | draft_id=\"${__value.raw}\""
      }
    ]
  }
}
