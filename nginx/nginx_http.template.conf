
server {
    listen 80;
    server_name ${DOMAIN} www.${DOMAIN};

    # Handle ACME challenge requests
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
        try_files $uri =404;
    }

    # Temporary: serve the app over HTTP until we get SSL
}
